# 简历版本差异审阅浏览器验收

- 日期：2026-08-06
- 分支：`feat/20260806-resume-version-review`
- 浏览器验收时实现提交：`672c88f`
- 页面：本地 `http://127.0.0.1:60394/`
- 模式：亮色模式，中文界面

## 数据隔离

验收使用临时隔离数据目录和中文合成简历。通过页面复制主简历、编辑岗位意向、联系方式、工作经历和技能，生成并保存岗位版本；另有一个中文候选简历用于切换基准。验收结束后由同一 harness 的 `finally` 清理进程树、端口、临时目录和环境变量，未使用用户真实简历。

## 验收结果

1. 简历库显示三个已保存版本，岗位版本可通过“对比版本”打开只读审阅。
2. 首次打开时自动选择父简历，并显示真实标题、`#ID` 和“主简历、父版本”关系。
3. 手动切换到第二个中文候选版本后，基准摘要和差异内容重新计算，未自动改回父简历。
4. 对比结果只比较已保存的 `content_json`，显示新增、删除、修改数量、中文模块、JSON Pointer 路径及修改前后原文。
5. 工作经历长文本显示“展开完整内容”；实际展开后可读到完整内容，再次点击可折叠。
6. 页面 URL 在打开、切换基准、展开/折叠和关闭期间保持不变。

## 网络与清理审计

- 从点击“对比版本”前开始记录 CDP `Network.requestWillBeSent` 事件，直到切换基准、展开/折叠和关闭完成：新增请求为 0。
- 对比期间未产生 Resume 写入、AI 请求、导航或外部 URL 请求；验收 harness 只提供本地静态资源和本地 `/api`。
- 完成标记写入后，日志出现 `CLEANED`；端口 `60394` 已释放，harness 进程已结束，临时数据目录已删除。

## 合并后浏览器回归

- 回归提交：`cc6f36c`，本地 `main`
- 模式：亮色模式，中文界面；使用临时隔离数据目录和三份合成简历
- 流程：复制中文主简历，编辑标题、求职意向、联系方式、工作经历、技能和长文本并保存；打开版本对比；确认父简历默认基准；切换到中文备用版本；展开并折叠长文本；关闭 Drawer
- 结果：默认父简历、手动切换基准、摘要、JSON Pointer、修改前后原文、折叠预览和展开全文均符合预期
- 网络审计：对比操作期间 `Network.requestWillBeSent` 为 0，导航事件为 0；页面加载期间仅有本地静态资源和本地 `/api` 请求，未访问外部 URL、AI Provider 或招聘平台
- 清理：同一 harness 的 `finally` 输出 `CLEANED`；端口 `60394`、临时目录和环境变量均已清理
