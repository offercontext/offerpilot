# README 产品概览重写设计

**基线：** `main@9ee97a8`

**设计状态：** 已完成用户确认，尚未实施。

## 1. 目标

将当前偏技术清单的 README 重写为普通求职者能在约十五秒内理解的产品首页：OfferPilot 是什么、能帮助完成哪些求职工作、Pilot 如何协作，以及如何快速开始。

README 不是飞书产品说明书、完整功能手册、路线图或架构文档。它应简短、准确、可验证，不为营销而宣称尚未发布或未通过验收的能力。

## 2. 读者与产品口径

目标读者是普通求职者，而不是仓库维护者或后端贡献者。README 使用中文为主、英文精简版为辅。

产品口径固定为：

- OfferPilot 是本地优先的 AI 求职工作台；
- 用户可管理简历、投递、岗位评估、材料、面试准备、模拟面试、复盘、知识、Offer 与谈薪；
- Pilot 在当前上下文中协作，写入前需要用户确认；
- 产品给出建议、帮助整理和准备，不自动投递、不自动执行外部动作，也不替用户决定接受或拒绝 Offer；
- 本地数据使用 SQLite；AI 仅在用户配置 Provider 后、由用户主动触发时使用。

不得以 README 宣传尚未合入主分支的 Offer 谈薪重构、Application Journey 面板、外部薪酬检索、自动投递、Offer 排名或录用预测。

## 3. 信息架构

README 的中文主段严格控制为以下八个区域：

1. **标题与一句话定位**：`OfferPilot — 本地优先的 AI 求职工作台`，后接一句面向结果的说明。
2. **它能帮你做什么**：一小段说明与五项短能力，不写功能穷举。
3. **真实界面**：五张中文、亮色、宽屏的真实本地部署截图；每张一行说明。
4. **Pilot 有何不同**：两到三句话说明上下文、建议与人工确认；不写模型供应商、内部工具或实现细节。
5. **快速开始**：Docker 和源码运行两条最短路径；命令真实可用。
6. **隐私与边界**：本地 SQLite、用户自带 AI 配置、无自动投递/替用户决策。
7. **精简英文版**：英文定位、能力概览、最短启动命令、隐私边界和许可证链接。
8. **许可证**：链接 AGPLv3，不在 README 中展开法律解释。

移除或移至其他文档：长 CLI 清单、完整 JSON 配置样例、技术栈表、历史架构调整、内部阶段路线图、未完成能力列表和重复的 Docker smoke 说明。

## 4. 截图策略

截图必须来自实际本地部署，而非设计稿、拼图、窄屏模拟或手工伪造数据页面。使用中文候选人“筱哲”的合成案例，亮色模式、宽屏视口、无密钥或真实个人信息。

截图统一存放为版本化 PNG：

```text
docs/assets/readme/2026-08-01/
  01-workspace-overview.png
  02-application-materials.png
  03-pilot-confirmation.png
  04-interview-practice.png
  05-offer-negotiation.png
```

每张图须满足：

- 宽屏截图、亮色模式、中文固定文案可读；
- 没有空白截图、裁切断裂、加载骨架、错误弹窗、浏览器栏或调试信息；
- 不暴露 API key、Token、配置文件路径、用户真实信息、Provider 原文或模型诊断；
- 显示实际已发布到 `main` 的功能；
- 经过逐张视觉检查，再被 README 引用。

五张图的用途固定为：

| 文件 | 场景 | README 一行说明 |
| --- | --- | --- |
| `01-workspace-overview.png` | 工作台与核心导航 | 在一个本地工作台中查看求职进展。 |
| `02-application-materials.png` | 投递详情、岗位评估或材料包 | 围绕一条投递整理事实、评估与材料。 |
| `03-pilot-confirmation.png` | Pilot 的用户提问、回答和人工确认卡 | Pilot 可以协作，但不会绕过你的确认。 |
| `04-interview-practice.png` | 面试准备、模拟面试或复盘 | 在面试前练习，在面试后复盘沉淀。 |
| `05-offer-negotiation.png` | 现有 Offer 中心与谈薪教练 | 比较已知 Offer 事实，并为沟通做准备。 |

截图不是逐功能教程；详细演示继续由飞书产品说明书承担。

## 5. 文案边界

中文标题与能力项使用短句：

- 管理简历与投递；
- 评估岗位匹配、准备投递材料；
- 准备面试、进行文本模拟与复盘；
- 汇总已确认的经验与知识；
- 比较 Offer 已知事实，准备谈薪沟通。

Pilot 说明使用“在当前上下文中协作”“展示建议和确认卡”“由你决定是否执行”等措辞。不得使用“自动完成投递”“替你筛选最优 Offer”“保证拿到 Offer”“智能决策”“无需确认”等表述。

用户的公司、岗位、简历标题、Offer 备注、证据摘录和 AI 可验证内容保留原文，不被 README 文案强制翻译或改写。

## 6. 实施与验证

1. 从当前 `main` 创建文档分支；不触碰其他开发分支或根工作树未提交内容。
2. 使用已有本地配置的副本和隔离数据目录部署；仅创建中文合成案例“筱哲”，不修改用户真实数据。
3. 在亮色宽屏实际操作中生成五张截图；涉及 Pilot 时必须展示用户提问、Pilot 回答和确认卡，而非仅展示侧栏空态。
4. 将图片放入 `docs/assets/readme/2026-08-01/`，以相对路径嵌入 README，并替换旧的中英文重复内容。
5. 使用 GitHub Markdown 预览或本地渲染逐张检查图片比例、可读性、相对路径和 README 首屏信息层级。
6. 运行至少 `git diff --check main..HEAD`；若 README 命令、Docker 或启动方式变更，运行对应验证并如实报告。
7. 提交 README、截图及验证记录；不在 README 中宣称未验证的功能或真实 AI 结果。

## 7. 不做

- 不重写产品 UI、布局或业务代码；
- 不修改飞书产品说明书；
- 不在 README 放置长篇产品教程、所有 API、CLI 或配置项；
- 不截取窄屏、深色模式、测试 mock、未完成加载或包含敏感信息的页面；
- 不复制完整飞书文档内容到 GitHub README。
