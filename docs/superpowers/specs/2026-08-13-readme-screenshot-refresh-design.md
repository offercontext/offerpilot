# README 产品截图刷新设计

状态：已批准并执行

## 目标

使用当前 `main`、实际本地部署和 README 既有中文候选人“筱哲”案例，替换 2026-08-01 的五张旧截图。截图必须来自亮色宽屏真实页面，不使用设计稿，不泄露密钥或真实个人数据。

## 截图矩阵

1. 工作台：展示本周求职作战台、今日行动与 Haru Pilot。
2. 投递与材料：展示“云栖智能（演示）· AI 应用工程师”的投递详情与材料工作区。
3. Pilot 确认：展示筱哲通过 Pilot 新增同一投递时的人工确认卡，并保留 Haru。
4. 面试练习：展示该岗位的面试入口和文本练习工作区。
5. Offer 与谈薪：展示同一案例 Offer、谈薪入口和 Pilot 上下文。

## 边界与验收

- 使用隔离数据目录，复用本机实际 Provider 配置但不在截图或日志中暴露密钥。
- 截图不依赖外部网站，不触发自动投递、外部消息或未经确认的业务写入。
- 每张 PNG 不小于 1440×800，亮色、中文、单视口，逐张回读检查裁切、空白、遮挡和文字清晰度。
- README 保持简洁，只更新说明与图片路径；旧截图保留在历史提交中，不在新版 README 引用。
- 更新自动化测试，固定新版截图目录、数量、尺寸与基础视觉多样性。
