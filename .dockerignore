# Build artifacts we never want in the Docker build context
oc
oc.exe
*.exe
*.dll
*.so
*.dylib

# Local data (may contain user keys / db)
*.db
*.db-journal
*.db-wal
*.db-shm
config.json
.offerpilot/

# Frontend build deps + outputs (rebuilt in-image)
web/node_modules/
web/dist/
web/*.tsbuildinfo

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Git
.git/